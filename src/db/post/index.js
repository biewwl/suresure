import { createBet } from "./createBet";
import { createBetDetail } from "./createBetDetail";
import { createBonus } from "./createBonus";
import { createMultipleBet } from "./createMultipleBet";
import { saveFullBet } from "./saveFullBet";

export const post = {
  createBet,
  createBetDetail,
  createBonus,
  createMultipleBet,
  saveFullBet
};
